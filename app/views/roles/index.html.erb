<div class="row">
	<div class="col-md-8"> 
		<div class="row">
			<div class="col-lg=8 col-md=8"> 
			</div>											
			</br>
		</div>
	</div>
	<div class="col-md-2 col-lg-2">
		<%= link_to "ADD ROLE", new_role_path, :class => 'btn btn-primary'%>
	</div>
	<center>
		<table class="table table-striped table-hover ">	
			<th>
				<tr style = "background-color: #def">
				<td>Role</td>
				<td></td>
				<td></td>
				</tr>
		  </th>
			
			<% @roles.each do |role|%>
				<tr>
					<td><%= role.role%></td>
					<td><%= link_to "edit",edit_role_path(role.id)%></td>
					<td><%= link_to "delete",role_path(role.id), :method => 'delete'%></td>
				</tr>
			<%end%>
		</table>
	</center>
</div>
