

<div class="container">
 	<div class="well">
  	<div class="row"> 
    	<div class="col-lg-10 ">	      	
      	<%= form_for :role, :url => role_path(@role.id), method: 'patch' do |t| %>
					<%= error_messages_for(@role) %>
					<center>
						<table class="table table-striped table-hover">
							<tr>
								<th><label for="Name">Name</label></th>
								<td><%= t.text_field :role%></td>
							</tr>
							<tr>
								<th></th>
								<td><%= t.submit "Edit", :class => "btn btn-primary"%></td>
							</tr>
						</table>
					</center>      	
    		<%end%>
    		<%= link_to "Back", roles_path, :class => "btn btn-primary"%>
    	</div>
  	</div>
 	</div>    
</div>   


